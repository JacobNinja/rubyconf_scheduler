<div id="lightning_talks" class="well">
<span class="header">Lightning Talks <%= link_to 'Submit new session', new_lightning_talk_path %></span>
<table class="table">
  <tr>
    <th>Speaker Name</th>
    <th>Title</th>
    <th>Description</th>
  </tr>

<% @lightning_talks.each do |session| %>
  <tr>
    <td><%= session.name %></td>
    <td><%= session.title %></td>
    <td><%= session.description %></td>
  </tr>
<% end %>
</table>

</div>

<div id="bofs" class="well">
  <span class="header">Birds of a Feather <%= link_to 'Submit new session', new_bof_path %></span>

<table class="table">
  <tr>
    <th>Speaker Name</th>
    <th>Title</th>
    <th>Description</th>
  </tr>

<% @bofs.each do |session| %>
  <tr>
    <td><%= session.name %></td>
    <td><%= session.title %></td>
    <td><%= session.description %></td>
  </tr>
<% end %>
</table>

</div>